<% unless current_user == user %>
  <% if Relation.find_by(followed_id: user.id) %>
    <%= link_to "フォローをやめる",user_relation_path(user.id),method: :delete, remote: true %>
  <% else %>
    <%= link_to "フォローする",user_relation_path(user.id),method: :post, remote: true %>
  <% end %>
<% end %>