<div class="profile-container">
  <%= render "profile", user: @user %>
  <div class="posts">
    <h2>Photo  <i class="fas fa-camera"></i></h2>
    <% if @user.photo_posts.count > 9 %>
      <%= link_to my_post_user_path(@user) do %>
        <h3 class="text-right posts__more">..all <i class="fas fa-angle-double-right"></i></h3>
      <% end %>
    <% end %>
    <%= render "photo_posts/index", photo_posts: @photo_posts%>
  </div>
</div>

